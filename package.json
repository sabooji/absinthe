{
  "name": "@syntaxa/absinthe",
  "version": "1.23.0",
  "description": "A minimal framework for creating AWS Lambda HTTP applications.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --out-dir dist/",
    "prepublish": "npm version minor && npm run build && git add . && git commit -m 'Publishing...'"
  },
  "devDependencies": {
    "babel-cli": "^6.2.0",
    "babel-core": "^6.2.1",
    "babel-preset-es2015": "^6.1.18"
  },
  "author": "",
  "license": "ISC",
  "babel": {
    "presets": [
      "es2015"
    ]
  }
}
